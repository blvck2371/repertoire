# Docker Backend

Stack backend : MongoDB + API Node.js.

## Lancer uniquement le backend

```bash
docker compose -f docker/backend/docker-compose.yml up -d
```

- API : http://localhost:3001
- MongoDB : localhost:27017
