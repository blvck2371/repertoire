# HTTPS + Let's Encrypt (Traefik)
# Copier vers .env et ajouter ces variables (ou les fusionner avec ton .env existant)

# Domaine de l'application (doit pointer vers l'IP du Droplet)
DOMAIN=repertoire.example.com

# Email pour Let's Encrypt (notifications de renouvellement)
LETSENCRYPT_EMAIL=admin@example.com
