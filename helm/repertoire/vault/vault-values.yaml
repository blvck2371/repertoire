# Vault Helm values - mode dev pour développement
# Référence: https://developer.hashicorp.com/vault/docs/platform/k8s/helm/configuration

global:
  enabled: true

server:
  dev:
    enabled: true
    devRootToken: "root"
  # Standalone pour prod (décommenter et désactiver dev)
  # standalone:
  #   enabled: true
  #   config: |
  #     storage "file" {
  #       path = "/vault/data"
  #     }
  #     listener "tcp" {
  #       tls_disable = 1
  #       address = "[::]:8200"
  #       cluster_address = "[::]:8201"
  #     }

injector:
  enabled: true
  replicas: 1

ui:
  enabled: true
