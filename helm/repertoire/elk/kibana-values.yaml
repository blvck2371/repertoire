# Kibana – ressources minimales
resources:
  requests:
    cpu: "50m"
    memory: "256Mi"
  limits:
    cpu: "200m"
    memory: "512Mi"

elasticsearchHosts: "http://elasticsearch-master:9200"

service:
  type: ClusterIP
  port: 5601
